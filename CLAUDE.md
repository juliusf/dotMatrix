this repo contains a implementation of a DMG gameboy emulator.

code goes to src
general docs about the gameboy are in docs

build the program with make
additionaly, the program can be built with "make debug". It enables additional debug output.
